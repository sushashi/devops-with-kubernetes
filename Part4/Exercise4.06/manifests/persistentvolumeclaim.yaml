apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: project
  name: project-claim
spec:
  storageClassName: files-project-pv
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi